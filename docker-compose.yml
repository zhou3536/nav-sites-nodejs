services:
  nav-sites:
    build: .
    container_name: nav-sites
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - AUTH_USERNAME=admin
      - AUTH_PASSWORD=admin123
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
